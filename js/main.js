var w = screen.width;
if (w > 768) {
    function myFunction() {
        var copyText = "+40735956217";
        navigator.clipboard.writeText(copyText);
        alert("Phone Number copied to clipboard");
    }
}